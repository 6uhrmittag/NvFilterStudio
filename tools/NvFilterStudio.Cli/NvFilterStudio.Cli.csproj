<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <!-- Short, because it is meant to be typed. Shipped alongside the GUI so
         someone filing a bug can run one command instead of hunting for
         version numbers by hand. -->
    <AssemblyName>nvfs</AssemblyName>
    <RootNamespace>NvFilterStudio.Cli</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\NvFilterStudio.Core\NvFilterStudio.Core.csproj" />
  </ItemGroup>

</Project>
